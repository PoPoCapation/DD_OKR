<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.bugstack.infrastructure.dao.IOkrObjectiveUserDao">

    <resultMap id="BaseResultMap" type="cn.bugstack.infrastructure.dao.po.OkrObjectiveUserPO">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="objective_id" property="objectiveId"/>
        <result column="createtime" property="createtime"/>
        <result column="updatetime" property="updatetime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_id, objective_id, createtime, updatetime
    </sql>

    <insert id="insert" parameterType="cn.bugstack.infrastructure.dao.po.OkrObjectiveUserPO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO okr_objective_user
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="userId != null">user_id,</if>
            <if test="objectiveId != null">objective_id,</if>
        </trim>
        <trim prefix="VALUES(" suffix=")" suffixOverrides=",">
            <if test="userId != null">#{userId},</if>
            <if test="objectiveId != null">#{objectiveId},</if>
        </trim>
    </insert>

    <select id="queryById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM okr_objective_user
        WHERE id = #{id}
    </select>

    <update id="update" parameterType="cn.bugstack.infrastructure.dao.po.OkrObjectiveUserPO">
        UPDATE okr_objective_user
        <set>
            <if test="userId != null">user_id = #{userId},</if>
            <if test="objectiveId != null">objective_id = #{objectiveId},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="java.lang.Long">
        DELETE FROM okr_objective_user WHERE id = #{id}
    </delete>

</mapper>
